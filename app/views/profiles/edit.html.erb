<h2>aboutyou</h2>
<p>Whether you're a new parent seeking advice or an experienced caregiver looking to share your wisdom, our platform offers the perfect space to learn, grow, and thrive together. Tell us just a little bit more about yourself so that we cann best match you to a commmunity.</p>
<%= simple_form_for(current_user, as: :user, url: profile_path, html: { method: :patch }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

    <%= f.input :due_date,
                required: true,
                input_html: { autocomplete: "due date" } %>
    <%= f.input :post_code,
                required: true,
                input_html: { autocomplete: "postcode" } %>
    <%= f.input :bio,
              required: true,
              input_html: { autocomplete: "short bio to introduce yourself" } %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Continue" %>
  </div>
<% end %>

<%= link_to "Back", :back %>
